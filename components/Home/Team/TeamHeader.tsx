import classes from './TeamHeader.module.scss';

const TeamHeader = () => {
  return(
  <div className={classes.header}>
    <h2 className={classes.header__title}>Poznaj nasz zespół</h2>
    <span></span>
  </div>);
};

export default TeamHeader;